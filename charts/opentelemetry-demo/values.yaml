# This is the main values file for the OpenTelemetry Demo
# Additional comments and validation notes have been added for clarity

components:
  frontend:
    enabled: true
    servicePort: 8080
    # Shipping service URL configuration
    # Note: Must include proper HTTP scheme for valid service communication
    shippingServiceUrl: "http://shipping:8080/get-quote"  # Updated with proper HTTP scheme
